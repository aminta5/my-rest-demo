package com.example.mavenDemoRest.services;

/*import com.example.mavenDemoRest.controllers.PostController;
import com.example.mavenDemoRest.model.Location;
import com.example.mavenDemoRest.model.Post;
import com.example.mavenDemoRest.model.User;

import java.util.List;

public class PostService {
    private static PostService instance = new PostService();
    private static PostController pController = new PostController();

    //private constructor
    private PostService() {}

    public static PostService getInstance(){
        return instance;
    }

    // redesign in builder pattern or args constructor
    public Post createPost(long id, String title, String description, User user, Location location) {
        Post post = new Post();
        //post.setId(id);
        post.setTitle(title);
        post.setDescription(description);
        post.setUser(user);
        post.setLocation(location);

        return post;
    }

    public List<Post> getPosts(){
        return pController.getPosts();
    }
}*/
